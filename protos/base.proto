syntax = "proto3";

option go_package = "/base/pbs";

package base;

service BaseService {
  // A simple unary RPC.
  rpc Ping(Input) returns (Output) {}
}

message Input {
  string Input = 1;
}

message Output {
    string Output = 1;
}